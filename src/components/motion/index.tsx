'use client';

import { 
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  useScroll,
  useInView
} from 'framer-motion';

// Re-export specific components and hooks from framer-motion
export { 
  motion,
  AnimatePresence,
  useAnimation,
  useMotionValue,
  useTransform,
  useScroll,
  useInView
};
